services:
  model:
    #image: mamadoudev/fastapi:v2
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: fastapi
    ports:
      - "8000:8000"
  streamlit:
    image: mamadoudev/streamlit-app:v2
    build: 
      context: ./streamlit_app
      dockerfile: Dockerfile
    container_name: streamlit
    ports:
      - "8501:8501"
    depends_on:
      - model
    environment:
      - API_URL=http://model:8000